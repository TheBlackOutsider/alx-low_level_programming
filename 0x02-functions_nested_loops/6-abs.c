#include "main.h"

/**
 * _abs - To compute the absolute value of an integer pass like parameter
 * @i: value use to find the absolute value
 * Return: the absolute value.
 */
int _abs(int i)
{
	if (i < 0)
		return (-i);
	return (i);
}

