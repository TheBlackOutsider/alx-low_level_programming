#include "main.h"

/**
 * add - To sum two integers
 * @a: first value which is to be used for sum.
 * @b: second value which is to be use for sum.
 * Return: int (Sum)
 */
int add(int a, int b)
{
	return (a + b);
}

